import React from 'react';

const NonUserCart = ({product, handleDelete}) => {
    console.log(product);
    return (
        <div>
            hi
        </div>
    );
};

export default NonUserCart;